<!--  <?php
if ($_SERVER["REQUEST_METHOD"] == "GET") {
    $name = isset($_GET['name']) ? $_GET['name'] : '';
    $email = isset($_GET['email']) ? $_GET['email'] : '';
    $resume = isset($_GET['resume']) ? $_GET['resume'] : '';
    $message = isset($_GET['message']) ? $_GET['message'] : '';
   
    echo "Name: $name <br>";
    echo "Email: $email <br>";
    echo "Resume: $resume <br>";
    echo "Email: $message <br>";
   
}
?> -->
//For this to work you need to run upload the code in 000webhost.com and run it over there. When you submit the form it will give you back the input you have given  